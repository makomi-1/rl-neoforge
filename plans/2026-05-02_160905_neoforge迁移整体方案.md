# NeoForge 迁移整体方案

生成时间：2026-05-02 16:09:05

## 任务背景

目标是在 `D:\OpenProjects\RedstoneLink\rl-neoforge-1.21.1` 上，将 `D:\OpenProjects\RedstoneLink\rl-release-no-mixin` 的 Fabric 1.21.1 实现迁移到 NeoForge 21.1.219。
本次迁移遵守以下硬约束：

- 严格遵守 Fabric 版的设计与语义，不主动引入功能改造或行为变化。
- 迁移时优先保持代码结构、命名、模块边界与数据语义一致。
- 如存在平台差异，仅替换平台接入层，不重写业务层。
- 贴图、模型、语言文件、配置、loot table、blockstate 等资源如有缺口，可直接从 Fabric 侧复制。
- 迁移过程按阶段推进，每阶段以“该部分编译通过”为验收标准。
- 最终验收为客户端冒烟成功。

## 现状分析

### 代码体量与平台耦合分布

- Fabric 项目 `src/main/java + src/client/java` 共 371 个 Java 文件。
- 其中约 301 个文件可视为平台中立或仅依赖 Mojang/通用代码。
- 约 70 个文件直接依赖 Fabric API，主要集中在：
  - 入口与 datagen
  - 注册层
  - 网络注册与收发
  - 服务端/客户端事件
  - 客户端渲染、按键、HUD、世界交互
  - 配置路径解析

### 平台差异分层

1. 构建与元数据层
- Fabric：`fabric.mod.json`、Loom、Fabric datagen 入口
- NeoForge：`neoforge.mods.toml`、ModDevGradle、`@Mod` 入口、事件总线驱动 datagen

2. 注册层
- Fabric：`Registry.register` + `FabricBlockEntityTypeBuilder` + `FabricItemGroup`
- NeoForge：`DeferredRegister`、`DeferredBlock/DeferredItem`、`BlockEntityType.Builder`、事件式创造标签填充

3. 生命周期与事件层
- Fabric：`ModInitializer`、`ClientModInitializer`、`ServerTickEvents`、`ClientTickEvents`、`ServerLifecycleEvents`、`PlayerBlockBreakEvents`、`UseBlockCallback` 等
- NeoForge：`@Mod` 构造注入 `IEventBus`、`NeoForge.EVENT_BUS`、`RegisterClientCommandsEvent`、`TickEvent`、`ServerStartedEvent/StoppingEvent`、交互事件等

4. 网络层
- Fabric：`PayloadTypeRegistry`、`ServerPlayNetworking`、`ClientPlayNetworking`
- NeoForge：`RegisterPayloadHandlersEvent`、`PayloadRegistrar`、payload handler 注册与上下文调度

5. 客户端渲染与输入层
- Fabric：`BlockRenderLayerMap`、`HudElementRegistry`、`WorldRenderEvents`、`KeyBindingHelper`、客户端消息事件
- NeoForge：客户端总线事件、`RegisterKeyMappingsEvent`、GUI/HUD/屏幕事件、`RenderLevelStageEvent`

6. 配置层
- Fabric：`FabricLoader.getInstance().getConfigDir()` + 自定义 `properties` 解析
- NeoForge：通过 `FMLPaths.CONFIGDIR` 获取配置目录，保留现有自定义解析与快照结构

7. 数据生成层
- Fabric：`DataGeneratorEntrypoint` + `FabricRecipeProvider`
- NeoForge：`GatherDataEvent` + NeoForge/Minecraft 官方 provider 体系

## 差异映射策略

### 总策略

采用“先平台骨架，后公共代码，最后平台接入层”的迁移方式：

1. 先让 NeoForge 工程具备稳定的构建、运行、资源装载与主入口。
2. 再迁移绝大多数平台中立类，使公共业务层尽快进入 NeoForge 编译图。
3. 对 Fabric 依赖点建立 NeoForge 适配层，逐类替换。
4. 每次只推进一个层级，避免整仓复制后出现大面积不可诊断错误。

### 各层实现原则

1. 业务类原则
- `com.makomi.data`、多数 `block`、`block.entity`、`item`、`util`、`menu`、`advancement`、`command` 业务装配代码尽量原样迁移。
- 若仅因平台入口或注册方式不同导致编译失败，优先改平台接入点，不改业务语义。

2. 注册类原则
- 将 Fabric 风格静态注册表改为 NeoForge 注册接线。
- 对外暴露的符号命名保持一致，避免级联修改业务代码。
- 如有必要，引入 `Supplier` 或兼容辅助层，但在使用侧尽量保持 Fabric 原调用方式。

3. 网络类原则
- 保持现有 payload 结构、字段语义、handler 分层与主线程切换逻辑。
- 替换的仅是注册 API 与上下文对象。

4. 客户端类原则
- 保持既有 GUI、世界外显、快捷键、HUD、网页桥接逻辑不变。
- 替换事件注册、包收发、渲染挂点、按键注册方式。

5. 配置类原则
- 保留 `properties` 文件格式、解析器、快照模型与访问门面。
- 仅调整配置目录解析与生命周期接入方式。

6. 资源类原则
- 资源优先整体复制。
- 若 NeoForge 对个别资源键路径要求不同，再做最小修正。

## 分阶段实施计划

### 阶段 0：工程基线确认（已完成）

目标：
- 保持当前 NeoForge 21.1.219 工程可执行 `gradlew tasks`
- 固化 `AGENTS.md`、本地 `skills`、工作区和分支校验到当前仓库

验收标准：
- `.\gradlew.bat tasks --no-daemon` 成功

阶段状态：
- 已完成 NeoForge 21.1.219 工程初始化与 `main` 分支/工作区校验规则落地。
- 已完成 `AGENTS.md`、本地 `skills` 复制与当前仓库路径修正。

### 阶段 1：公共目录与资源底座迁移（已完成）

目标：
- 将 Fabric 侧非平台资源整体迁入 NeoForge
- 建立 `src/main/resources`、`src/client/java`、`src/test/java` 等目录结构

验收标准：
- 资源复制后 `compileJava` 仍通过
- `processResources` 成功

阶段状态：
- 已按正确迁移基线 `D:\OpenProjects\RedstoneLink\rl-release-no-mixin` 同步源码与资源目录。
- 已将 `src/client/java` 接入当前 NeoForge 编译图，并暂时排除 `com/makomi/datagen/**` 以聚焦运行期迁移。

### 阶段 2：注册层迁移（已完成）

目标：
- 完成 Block、Item、BlockEntity、MenuType、CreativeTab 的 NeoForge 注册接线

验收标准：
- 注册层相关类全部编译通过
- `compileJava` 成功

阶段状态：
- 当前注册层保持 Fabric 风格静态外观，但已在 NeoForge 工程中完成编译接入。
- 如后续运行期发现必须替换为更原生的 `DeferredRegister` 方案，只在平台装配层收口，不主动改动业务调用面。

### 阶段 3：公共业务层迁移（已完成）

目标：
- 将绝大部分平台中立业务类接入 NeoForge 编译图

验收标准：
- 公共业务层编译通过
- `compileJava` 成功

阶段状态：
- 主端业务类已整体接入 NeoForge 编译图。
- 当前仍以“尽量不改业务层、优先补平台兼容桥”为实现原则。

### 阶段 4：配置与命令接入层迁移（已完成）

目标：
- 迁移配置加载入口、命令入口、参数类型注册

验收标准：
- 配置与命令层编译通过
- `compileJava` 成功

阶段状态：
- 已通过 `FabricLoader -> FMLPaths` 与命令参数兼容注册桥打通当前主端配置/命令编译。

### 阶段 5：服务端事件与调度层迁移（已完成）

目标：
- 替换所有 Fabric 服务端生命周期事件、tick 事件、方块破坏事件、玩家连接事件

验收标准：
- 服务端事件层编译通过
- `compileJava` 成功

阶段状态：
- 已通过 NeoForge 事件总线转发 `ServerLifecycleEvents`、`ServerTickEvents`、`ServerEntityEvents`、`PlayerBlockBreakEvents`、`ServerPlayConnectionEvents`。
- 主端阶段验收已达成：`.\gradlew.bat compileJava --no-daemon --stacktrace` 成功。

### 阶段 6：网络层迁移（已完成）

目标：
- 完成 payload 注册、C2S/S2C handler 注册和上下文调度迁移

验收标准：
- 网络层编译通过
- `compileJava` 成功

阶段状态：
- C2S/S2C payload 注册、服务端 handler 分发、客户端 receiver 分发均已打通。
- 当前 NeoForge 兼容层保持 Fabric 风格 API 表面，业务 handler 初始化顺序保持与原版一致。

### 阶段 7：客户端入口、渲染、按键、HUD、交互迁移（已完成）

目标：
- 迁移客户端事件与渲染接入，使客户端可以成功启动

验收标准：
- 客户端相关源码编译通过
- `runClient` 可进入启动阶段

阶段状态：
- `RedstoneLinkClient` 已按 Fabric 原初始化顺序接到 NeoForge 客户端事件。
- 已补齐最小客户端兼容面：
  - `ClientPlayNetworking`
  - `ClientTickEvents`
  - `ClientReceiveMessageEvents`
  - `WorldRenderEvents / WorldRenderContext`
  - `AttackBlockCallback`
  - `UseBlockCallback`
  - `FabricLoader.getGameDir()`
- `quick_link_tool` 模型态已按原版设计收敛为 `sd/cp` 两档，已清理历史 `visualize/vs` 独立贴图态引用。
- `.\gradlew.bat runClient --no-daemon --stacktrace` 已可成功启动并正常退出，且不再出现 `fast_link_tool_vs` 缺贴图告警。

### 阶段 8：datagen 与测试接入（已完成）

目标：
- 补齐 NeoForge datagen 入口与测试编译/运行能力

验收标准：
- datagen 源码编译通过
- `runData` 可执行
- 测试至少完成稳定核心用例的可运行基建收口

阶段状态：
- `runData` 已成功。
- `compileTestJava` 已成功。
- `testCompileClasspath` 与 `testRuntimeClasspath` 已接入 NeoForge 主工程 classpath 与 JUnit 5。
- 已通过 NeoForge 官方 `unitTest` 接线补齐最小 FML 初始化环境，解决 `LoadingModList.get()` 为空导致的稳定核心测试批量失败问题。
- 已新增 `syncJUnitWorkspace`，将 `src/main` 与 `src/test/resources` 同步到 `build/minecraft-junit/src`，兼容旧测试夹具仍使用相对路径读取源码/资源的设计。
- `.\gradlew.bat test --no-daemon --stacktrace` 已成功。

### 阶段 9：客户端冒烟与收口（已完成）

目标：
- 完成最终客户端冒烟

验收标准：
- 客户端成功启动并完成最小冒烟

阶段状态：
- 已完成首轮客户端冒烟：
  - 模组初始化成功
  - 资源加载成功
  - 可进入客户端
  - 正常退出
- 在测试基建收口后，已完成最终回归与客户端复验：
  - `.\gradlew.bat test --no-daemon --stacktrace` 成功
  - `.\gradlew.bat runData --no-daemon --stacktrace` 成功
  - `.\gradlew.bat runClient --no-daemon --stacktrace` 成功
- `quick_link_tool` 继续保持 `sd/cp` 两档模型态，客户端启动日志不再出现历史 `fast_link_tool_vs` 缺贴图告警。

## 语义对齐说明

本次迁移严格保持 Fabric 设计语义：

- 节点语义继续保持 `triggerSource/core` 基线，与既有 `LinkNodeType.TRIGGER_SOURCE/CORE` 一一映射。
- 网络 payload、配置键、业务数据结构、命令树语义保持不变。
- 允许变化的仅是 NeoForge 平台接入方式，不改变业务规则。
- `quick_link_tool` 的物品模型态与原始迁移对象保持一致，仅保留 `sd/cp` 两档；`visualize` 仍作为观察工作流存在，但不再占用独立贴图态。

当前已知残余风险：

1. 当前迁移范围内无新增功能性残余风险。
2. 本轮未单独补跑 dedicated server 或物理世界功能回归；若后续进入联机/服务器专项阶段，应在当轮按“先构建、再同步 server jar”的规则单独复验。

## 原子步骤清单

1. 建立 `plans` 与 `docs_dev` 目录骨架。
2. 复制 Fabric 资源到底层 NeoForge 资源目录。
3. 迁移注册层并编译通过。
4. 迁移平台中立业务层并编译通过。
5. 迁移配置、命令、服务端事件并编译通过。
6. 迁移网络层并编译通过。
7. 迁移客户端层并编译通过，完成首轮客户端冒烟。
8. 打通 datagen 与测试编译链。
9. 修复 NeoForge `test` 运行期初始化问题。
10. 执行最终回归并再次完成客户端冒烟。

## 预期结果

- `rl-neoforge-1.21.1` 获得一份与 Fabric 版设计一致的 NeoForge 实现。
- 迁移过程分阶段可验收，每阶段都有明确编译或运行目标。
- 最终达到 NeoForge 客户端可启动、资源可显示、稳定核心测试可运行且 `test/runData/runClient` 全部通过的收口目标。
